<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="BrowseItemsPage"
             x:Class="Game.Views.ScoreIndexPage"
             >

    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal">
            <Label Text="Score Index" Style="{StaticResource HeaderStyle}" HorizontalOptions="CenterAndExpand"/>
            <Button Text="Create" Clicked="AddScore_Clicked" HorizontalOptions="End"/>
        </StackLayout>
    </NavigationPage.TitleView>

    <!--<ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="AddScore_Clicked" IconImageSource="icon_add.png"/>
    </ContentPage.ToolbarItems>-->

    <ContentPage.Content>
        <StackLayout Style="{StaticResource OuterBoxStack}">

            <ListView x:Name="DataListView" 
                    ItemsSource="{Binding Dataset}"
                    IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                    RefreshCommand="{Binding LoadItemsCommand}"
                    CachingStrategy="RecycleElement"
                    ItemSelected="OnItemSelected"
                    HasUnevenRows="true"
                    Margin="5,0,5,0"
                    HorizontalOptions="Center"
                    >
                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid RowSpacing="1"
                                  HorizontalOptions="CenterAndExpand"
                                  VerticalOptions="CenterAndExpand"
                                  >

                                <StackLayout HorizontalOptions="CenterAndExpand">

                                    <Frame 
                                    OutlineColor="Black" 
                                    Padding="1"
                                    >

                                        <StackLayout 
                                        BackgroundColor="Goldenrod"
                                        WidthRequest="200"
                                        MinimumHeightRequest="800"
                                        Padding="20"
                                        >

                                            <Label 
                                           Text="{Binding Name}" 
                                           Style="{StaticResource ListStyle}"
                                           HorizontalTextAlignment="Center"
                                           HorizontalOptions="CenterAndExpand"
                                           />

                                            <StackLayout 
                                           Orientation="Horizontal"
                                           HorizontalOptions="CenterAndExpand"
                                           >

                                                <Label 
                                               Text="Rounds:"
                                               Style="{StaticResource ListStyle}"
                                                />

                                                <Label 
                                                Text="{Binding RoundCount}" 
                                                Style="{StaticResource ListStyle}"
                                                />

                                            </StackLayout>

                                        </StackLayout>
                                    </Frame>
                                    <!--<Label Grid.Column="0" Grid.Row="0"
                                       Text="{Binding Name}" 
                                       Style="{StaticResource ListStyle}"/>

                                    <Label Grid.Column="0" Grid.Row="1"
                                       Text="Rounds:" 
                                       Style="{StaticResource ListStyle}"/>

                                    <Label Grid.Column="1" Grid.Row="1"
                                       Text="{Binding RoundCount}" 
                                       Style="{StaticResource ListStyle}"/>

                                    <Label Grid.Column="0" Grid.Row="2"
                                       Text="Score:" 
                                       Style="{StaticResource ListStyle}"/>

                                    <Label Grid.Column="1" Grid.Row="2"
                                       Text="{Binding ScoreTotal}" 
                                       Style="{StaticResource ListStyle}"/>-->
                                </StackLayout>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>